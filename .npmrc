# NestJS + Prisma đôi khi cần node_modules "phẳng" hơn để resolve đúng peer deps
# (vd @nestjs/* packages phụ thuộc lẫn nhau qua reflect-metadata/rxjs).
# Bật shamefully-hoist để tránh lỗi "cannot find module" khó chịu khi dùng pnpm.
shamefully-hoist=true
